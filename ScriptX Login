local service = 1768;  -- your service id, this is used to identify your service.
local secret = "5e2e48a6-e898-4b2a-bdd9-114b7e49b7fa";  -- make sure to obfuscate this if you want to ensure security.
local useNonce = true;  -- use a nonce to prevent replay attacks and request tampering.
local onMessage = function(message)  game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", { Text = message; }) end;


repeat task.wait(1) until game:IsLoaded() or game.Players.LocalPlayer;

local requestSending = false;
local fSetClipboard, fRequest, fStringChar, fToString, fStringSub, fOsTime, fMathRandom, fMathFloor, fGetHwid = setclipboard or toclipboard, request or http_request, string.char, tostring, string.sub, os.time, math.random, math.floor, gethwid or function() return game:GetService("Players").LocalPlayer.UserId end
local cachedLink, cachedTime = "", 0;
local HttpService = game:GetService("HttpService")

function lEncode(data)
	return HttpService:JSONEncode(data)
end
function lDecode(data)
	return HttpService:JSONDecode(data)
end
local function lDigest(input)
	local inputStr = tostring(input)


	local hash = {}
	for i = 1, #inputStr do
		table.insert(hash, string.byte(inputStr, i))
	end

	local hashHex = ""
	for _, byte in ipairs(hash) do
		hashHex = hashHex .. string.format("%02x", byte)
	end

	return hashHex
end
local host = "https://api.platoboost.com";
local hostResponse = fRequest({
	Url = host .. "/public/connectivity",
	Method = "GET"
});
if hostResponse.StatusCode ~= 200 or hostResponse.StatusCode ~= 429 then
	host = "https://api.platoboost.net";
end

function cacheLink()
	if cachedTime + (10*60) < fOsTime() then
		local response = fRequest({
			Url = host .. "/public/start",
			Method = "POST",
			Body = lEncode({
				service = service,
				identifier = lDigest(fGetHwid())
			}),
			Headers = {
				["Content-Type"] = "application/json"
			}
		});

		if response.StatusCode == 200 then
			local decoded = lDecode(response.Body);

			if decoded.success == true then
				cachedLink = decoded.data.url;
				cachedTime = fOsTime();
				return true, cachedLink;
			else
				onMessage(decoded.message);
				return false, decoded.message;
			end
		elseif response.StatusCode == 429 then
			local msg = "you are being rate limited, please wait 20 seconds and try again.";
			onMessage(msg);
			return false, msg;
		end

		local msg = "Failed to cache link.";
		onMessage(msg);
		return false, msg;
	else
		return true, cachedLink;
	end
end



cacheLink();

local generateNonce = function()
	local str = ""
	for _ = 1, 16 do
		str = str .. fStringChar(fMathFloor(fMathRandom() * (122 - 97 + 1)) + 97)
	end
	return str
end


for _ = 1, 5 do
	local oNonce = generateNonce();
	task.wait(0.2)
	if generateNonce() == oNonce then
		local msg = "platoboost nonce error.";
		onMessage(msg);
		error(msg);
	end
end

local copyLink = function()
	local success, link = cacheLink();

	if success then
		game:GetService("StarterGui"):SetCore("SendNotification",{
			Title = "ScriptX",
			Text = "SetToClipboard Key Website",
			Icon = ""
		})
		fSetClipboard(link);
	end
end

local copyDiscordLink = function()
	game:GetService("StarterGui"):SetCore("SendNotification",{
		Title = "ScriptX",
		Text = "SetToClipboard Discord Server Link",
		Icon = ""
	})
	fSetClipboard("https://discord.gg/wfBPYBnDxX");
end

local redeemKey = function(key)
	local nonce = generateNonce();
	local endpoint = host .. "/public/redeem/" .. fToString(service);

	local body = {
		identifier = lDigest(fGetHwid()),
		key = key
	}

	if useNonce then
		body.nonce = nonce;
	end

	local response = fRequest({
		Url = endpoint,
		Method = "POST",
		Body = lEncode(body),
		Headers = {
			["Content-Type"] = "application/json"
		}
	});

	if response.StatusCode == 200 then
		local decoded = lDecode(response.Body);
		if decoded.success == true then
			if decoded.data.valid == true then
				if useNonce then
					if decoded.data.hash == lDigest("true" .. "-" .. nonce .. "-" .. secret) then
						return true;
					else
						onMessage("failed to verify integrity.");
						return false;
					end    
				else
					return true;
				end
			else
				onMessage("key is invalid.");
				return false;
			end
		else
			if fStringSub(decoded.message, 1, 27) == "unique constraint violation" then
				onMessage("you already have an active key, please wait for it to expire before redeeming it.");
				return false;
			else
				onMessage(decoded.message);
				return false;
			end
		end
	elseif response.StatusCode == 429 then
		onMessage("you are being rate limited, please wait 20 seconds and try again.");
		return false;
	else
		onMessage("server returned an invalid status code, please try again later.");
		return false; 
	end
end


local verifyKey = function(key)
	if requestSending == true then
		onMessage("a request is already being sent, please slow down.");
		return false;
	else
		requestSending = true;
	end

	local nonce = generateNonce();
	local endpoint = host .. "/public/whitelist/" .. fToString(service) .. "?identifier=" .. lDigest(fGetHwid()) .. "&key=" .. key;

	if useNonce then
		endpoint = endpoint .. "&nonce=" .. nonce;
	end
	local response = fRequest({
		Url = endpoint,
		Method = "GET",
	});

	requestSending = false;

	if response.StatusCode == 200 then
		local decoded = lDecode(response.Body);
		if decoded.success == true then
			if decoded.data.valid == true then
				if useNonce then
					return true;
				else
					return true;
				end
			else
				if fStringSub(key, 1, 4) == "FREE_" then
					return redeemKey(key);
				else
					onMessage("key is invalid.");
					return false;
				end
			end
		else
			onMessage(decoded.message);
			return false;
		end
	elseif response.StatusCode == 429 then
		onMessage("you are being rate limited, please wait 20 seconds and try again.");
		return false;
	else
		onMessage("server returned an invalid status code, please try again later.");
		return false;
	end
end


local getFlag = function(name)
	local nonce = generateNonce();
	local endpoint = host .. "/public/flag/" .. fToString(service) .. "?name=" .. name;

	if useNonce then
		endpoint = endpoint .. "&nonce=" .. nonce;
	end

	local response = fRequest({
		Url = endpoint,
		Method = "GET",
	});

	if response.StatusCode == 200 then
		local decoded = lDecode(response.Body);
		if decoded.success == true then
			if useNonce then
				if decoded.data.hash == lDigest(fToString(decoded.data.value) .. "-" .. nonce .. "-" .. secret) then
					return decoded.data.value;
				else
					onMessage("failed to verify integrity.");
					return nil;
				end
			else
				return decoded.data.value;
			end
		else
			onMessage(decoded.message);
			return nil;
		end
	else
		return nil;
	end
end

-- Gui to Lua
-- Version: 3.2

-- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UIGradient = Instance.new("UIGradient")
local UICorner = Instance.new("UICorner")
local ScriptXName = Instance.new("TextLabel")
local UIGradient_2 = Instance.new("UIGradient")
local UITextSizeConstraint = Instance.new("UITextSizeConstraint")
local ImageButton = Instance.new("ImageButton")
local Frame_2 = Instance.new("Frame")
local UICorner_2 = Instance.new("UICorner")
local UIGradient_3 = Instance.new("UIGradient")
local Getkey = Instance.new("TextButton")
local UITextSizeConstraint_2 = Instance.new("UITextSizeConstraint")
local StrokeBlack = Instance.new("Frame")
local UICorner_3 = Instance.new("UICorner")
local Frame_3 = Instance.new("Frame")
local UICorner_4 = Instance.new("UICorner")
local UIGradient_4 = Instance.new("UIGradient")
local Checkkey = Instance.new("TextButton")
local UITextSizeConstraint_3 = Instance.new("UITextSizeConstraint")
local StrokeBlack_2 = Instance.new("Frame")
local UICorner_5 = Instance.new("UICorner")
local StrokeBlack_3 = Instance.new("Frame")
local UICorner_6 = Instance.new("UICorner")
local TextboxFrame = Instance.new("Frame")
local UICorner_7 = Instance.new("UICorner")
local UIGradient_5 = Instance.new("UIGradient")
local StrokeBlack_4 = Instance.new("Frame")
local UICorner_8 = Instance.new("UICorner")
local TextBox = Instance.new("TextBox")
local UIGradient_6 = Instance.new("UIGradient")
local UITextSizeConstraint_4 = Instance.new("UITextSizeConstraint")
local Exit = Instance.new("TextButton")
local UIGradient_7 = Instance.new("UIGradient")
local UITextSizeConstraint_5 = Instance.new("UITextSizeConstraint")

--Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

Frame.Parent = ScreenGui
Frame.AnchorPoint = Vector2.new(0.5, 0.5)
Frame.BackgroundColor3 = Color3.fromRGB(162, 0, 0)
Frame.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
Frame.Size = UDim2.new(0.255441755, 0, 0.359296471, 0)

UIGradient.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.17, Color3.fromRGB(43, 43, 43)), ColorSequenceKeypoint.new(0.31, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(0.52, Color3.fromRGB(189, 189, 189)), ColorSequenceKeypoint.new(0.56, Color3.fromRGB(227, 227, 227)), ColorSequenceKeypoint.new(0.70, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(0.78, Color3.fromRGB(189, 189, 189)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(40, 40, 40))}
UIGradient.Rotation = 270
UIGradient.Parent = Frame

UICorner.CornerRadius = UDim.new(0.140000001, 0)
UICorner.Parent = Frame

ScriptXName.Name = "ScriptXName"
ScriptXName.Parent = Frame
ScriptXName.AnchorPoint = Vector2.new(0.5, 0.5)
ScriptXName.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ScriptXName.BackgroundTransparency = 1.000
ScriptXName.BorderColor3 = Color3.fromRGB(0, 0, 0)
ScriptXName.BorderSizePixel = 0
ScriptXName.Position = UDim2.new(0.498989969, 0, 0.159792393, 0)
ScriptXName.Size = UDim2.new(0.457741469, 0, 0.239913404, 0)
ScriptXName.Font = Enum.Font.Kalam
ScriptXName.Text = "ScriptX"
ScriptXName.TextColor3 = Color3.fromRGB(255, 255, 255)
ScriptXName.TextScaled = true
ScriptXName.TextSize = 14.000
ScriptXName.TextWrapped = true

UIGradient_2.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_2.Rotation = 270
UIGradient_2.Parent = ScriptXName

UITextSizeConstraint.Parent = ScriptXName
UITextSizeConstraint.MaxTextSize = 68

ImageButton.Parent = Frame
ImageButton.AnchorPoint = Vector2.new(0.5, 0.5)
ImageButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageButton.BackgroundTransparency = 1.000
ImageButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.119999997, 0, 0.128999993, 0)
ImageButton.Size = UDim2.new(0.128654972, 0, 0.0944055915, 0)
ImageButton.Image = "rbxassetid://87826830808124"

Frame_2.Parent = Frame
Frame_2.AnchorPoint = Vector2.new(0.5, 0.5)
Frame_2.BackgroundColor3 = Color3.fromRGB(24, 143, 255)
Frame_2.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame_2.BorderSizePixel = 0
Frame_2.Position = UDim2.new(0.501253068, 0, 0.608434737, 0)
Frame_2.Size = UDim2.new(0.459482014, 0, 0.146853149, 0)
Frame_2.ZIndex = 5

UICorner_2.CornerRadius = UDim.new(0.140000001, 0)
UICorner_2.Parent = Frame_2

UIGradient_3.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_3.Rotation = 270
UIGradient_3.Parent = Frame_2

Getkey.Name = "Getkey"
Getkey.Parent = Frame_2
Getkey.AnchorPoint = Vector2.new(0.5, 0.5)
Getkey.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Getkey.BackgroundTransparency = 1.000
Getkey.BorderColor3 = Color3.fromRGB(0, 0, 0)
Getkey.BorderSizePixel = 0
Getkey.Position = UDim2.new(0.5, 0, 0.5, 0)
Getkey.Size = UDim2.new(0.699999988, 0, 0.699999988, 0)
Getkey.ZIndex = 5
Getkey.Font = Enum.Font.SourceSans
Getkey.Text = "Get Key"
Getkey.TextColor3 = Color3.fromRGB(255, 255, 255)
Getkey.TextScaled = true
Getkey.TextSize = 14.000
Getkey.TextStrokeColor3 = Color3.fromRGB(191, 191, 191)
Getkey.TextStrokeTransparency = 0.000
Getkey.TextWrapped = true

UITextSizeConstraint_2.Parent = Getkey
UITextSizeConstraint_2.MaxTextSize = 29

StrokeBlack.Name = "StrokeBlack"
StrokeBlack.Parent = Frame_2
StrokeBlack.AnchorPoint = Vector2.new(0.5, 0.5)
StrokeBlack.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack.BorderColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack.BorderSizePixel = 0
StrokeBlack.Position = UDim2.new(0.5, 0, 0.5, 0)
StrokeBlack.Size = UDim2.new(1.02999997, 0, 1.20000005, 0)

UICorner_3.CornerRadius = UDim.new(0.140000001, 0)
UICorner_3.Parent = StrokeBlack

Frame_3.Parent = Frame
Frame_3.AnchorPoint = Vector2.new(0.5, 0.5)
Frame_3.BackgroundColor3 = Color3.fromRGB(24, 143, 255)
Frame_3.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame_3.BorderSizePixel = 0
Frame_3.Position = UDim2.new(0.498746783, 0, 0.798756838, 0)
Frame_3.Size = UDim2.new(0.459482014, 0, 0.146853149, 0)
Frame_3.ZIndex = 5

UICorner_4.CornerRadius = UDim.new(0.140000001, 0)
UICorner_4.Parent = Frame_3

UIGradient_4.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_4.Rotation = 270
UIGradient_4.Parent = Frame_3

Checkkey.Name = "Checkkey"
Checkkey.Parent = Frame_3
Checkkey.AnchorPoint = Vector2.new(0.5, 0.5)
Checkkey.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Checkkey.BackgroundTransparency = 1.000
Checkkey.BorderColor3 = Color3.fromRGB(0, 0, 0)
Checkkey.BorderSizePixel = 0
Checkkey.Position = UDim2.new(0.5, 0, 0.5, 0)
Checkkey.Size = UDim2.new(0.699999988, 0, 0.699999988, 0)
Checkkey.ZIndex = 5
Checkkey.Font = Enum.Font.SourceSans
Checkkey.Text = "Confirm"
Checkkey.TextColor3 = Color3.fromRGB(255, 255, 255)
Checkkey.TextScaled = true
Checkkey.TextSize = 14.000
Checkkey.TextStrokeColor3 = Color3.fromRGB(191, 191, 191)
Checkkey.TextStrokeTransparency = 0.000
Checkkey.TextWrapped = true

UITextSizeConstraint_3.Parent = Checkkey
UITextSizeConstraint_3.MaxTextSize = 29

StrokeBlack_2.Name = "StrokeBlack"
StrokeBlack_2.Parent = Frame_3
StrokeBlack_2.AnchorPoint = Vector2.new(0.5, 0.5)
StrokeBlack_2.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_2.BorderColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_2.BorderSizePixel = 0
StrokeBlack_2.Position = UDim2.new(0.5, 0, 0.5, 0)
StrokeBlack_2.Size = UDim2.new(1.02999997, 0, 1.20000005, 0)

UICorner_5.CornerRadius = UDim.new(0.140000001, 0)
UICorner_5.Parent = StrokeBlack_2

StrokeBlack_3.Name = "StrokeBlack"
StrokeBlack_3.Parent = Frame
StrokeBlack_3.AnchorPoint = Vector2.new(0.5, 0.5)
StrokeBlack_3.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_3.BorderColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_3.BorderSizePixel = 0
StrokeBlack_3.Position = UDim2.new(0.5, 0, 0.5, 0)
StrokeBlack_3.Size = UDim2.new(1.02999997, 0, 1.02999997, 0)
StrokeBlack_3.ZIndex = 0

UICorner_6.CornerRadius = UDim.new(0.140000001, 0)
UICorner_6.Parent = StrokeBlack_3

TextboxFrame.Name = "TextboxFrame"
TextboxFrame.Parent = Frame
TextboxFrame.AnchorPoint = Vector2.new(0.5, 0.5)
TextboxFrame.BackgroundColor3 = Color3.fromRGB(139, 139, 139)
TextboxFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextboxFrame.BorderSizePixel = 0
TextboxFrame.Position = UDim2.new(0.501253068, 0, 0.362458885, 0)
TextboxFrame.Size = UDim2.new(0.609857976, 0, 0.162369102, 0)
TextboxFrame.ZIndex = 5

UICorner_7.CornerRadius = UDim.new(0.140000001, 0)
UICorner_7.Parent = TextboxFrame

UIGradient_5.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_5.Rotation = 270
UIGradient_5.Parent = TextboxFrame

StrokeBlack_4.Name = "StrokeBlack"
StrokeBlack_4.Parent = TextboxFrame
StrokeBlack_4.AnchorPoint = Vector2.new(0.5, 0.5)
StrokeBlack_4.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_4.BorderColor3 = Color3.fromRGB(0, 0, 0)
StrokeBlack_4.BorderSizePixel = 0
StrokeBlack_4.Position = UDim2.new(0.5, 0, 0.5, 0)
StrokeBlack_4.Size = UDim2.new(1.02999997, 0, 1.20000005, 0)

UICorner_8.CornerRadius = UDim.new(0.140000001, 0)
UICorner_8.Parent = StrokeBlack_4

TextBox.Parent = TextboxFrame
TextBox.AnchorPoint = Vector2.new(0.5, 0.5)
TextBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextBox.BackgroundTransparency = 1.000
TextBox.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextBox.BorderSizePixel = 0
TextBox.Position = UDim2.new(0.5, 0, 0.5, 0)
TextBox.Size = UDim2.new(0.800000012, 0, 0.800000012, 0)
TextBox.ZIndex = 5
TextBox.Font = Enum.Font.SourceSans
TextBox.PlaceholderColor3 = Color3.fromRGB(168, 168, 168)
TextBox.PlaceholderText = "Input Your Key"
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextScaled = true
TextBox.TextSize = 14.000
TextBox.TextWrapped = true

UIGradient_6.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_6.Rotation = 270
UIGradient_6.Parent = TextBox

UITextSizeConstraint_4.Parent = TextBox
UITextSizeConstraint_4.MaxTextSize = 37

Exit.Name = "Exit"
Exit.Parent = Frame
Exit.AnchorPoint = Vector2.new(0.5, 0.5)
Exit.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Exit.BackgroundTransparency = 1.000
Exit.BorderColor3 = Color3.fromRGB(0, 0, 0)
Exit.BorderSizePixel = 0
Exit.Position = UDim2.new(0.920100152, 0, 0.152540356, 0)
Exit.Size = UDim2.new(0.126914561, 0, 0.224994048, 0)
Exit.Font = Enum.Font.Kalam
Exit.Text = "X"
Exit.TextColor3 = Color3.fromRGB(255, 0, 0)
Exit.TextScaled = true
Exit.TextSize = 14.000
Exit.TextWrapped = true

UIGradient_7.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)), ColorSequenceKeypoint.new(0.48, Color3.fromRGB(91, 91, 91)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255))}
UIGradient_7.Rotation = 270
UIGradient_7.Parent = Exit

UITextSizeConstraint_5.Parent = Exit
UITextSizeConstraint_5.MaxTextSize = 68

Checkkey.MouseButton1Down:Connect(function() 
	if TextBox and TextBox.Text then

		local Verify = verifyKey(TextBox.Text)
		if Verify then
			ScreenGui:Destroy()
			loadstring(game:HttpGet("https://pastebin.com/raw/A4teZnb4"))()
		else
			print("Key Is in valid")
		end 
	end	
end)

ImageButton.MouseButton1Down:Connect(function() 
	copyDiscordLink()
end)

Getkey.MouseButton1Down:Connect(function() 
	copyLink()
end)

Exit.MouseButton1Down:Connect(function()
	if ScreenGui then
		ScreenGui:Destroy()
	end
end)
